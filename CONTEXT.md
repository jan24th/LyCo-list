# LyCo-list

LyCo-list 是供家庭或小团队共享待办事项与列表的 PWA。

## Language

**测试控制 API**：仅在隔离测试环境中建立、检查或清理可重复验收场景状态的 HTTP 接口；不属于产品 API 契约。
_Avoid_: E2E 测试 API, 测试后门

**短生命周期 E2E 环境**：为单次 E2E 验收而创建的独立部署环境，完成后应被销毁；与开发、验收和生产环境隔离。
_Avoid_: 测试 stage, 共享测试环境

**测试运行**：一个 E2E 测试用例创建的唯一状态边界，拥有其测试身份、场景数据和清理权限。
_Avoid_: 测试会话, 测试批次, worker 运行

**具名场景**：由系统预先定义、可装载到测试运行中的可重复验收状态；调用方不能提交任意持久化数据。
_Avoid_: 任意 seed 数据, 测试夹具 JSON

**测试控制调用者**：以受限 CI IAM 角色运行、可调用测试控制 API 的自动化运行器；浏览器和产品用户不属于此角色。
_Avoid_: 测试管理员用户, 浏览器测试凭证

**测试运行回收**：先显式、幂等地清理测试运行，再由短 TTL 和后台清理兜底的回收策略。
_Avoid_: 只删数据库记录, 仅环境销毁

**测试身份**：由一个测试运行专属拥有的 Cognito 产品用户及其短期 access token，用于调用登录后的业务 API；它不同于测试控制调用者。
_Avoid_: 测试管理员, 控制凭证

**串行 E2E 执行**：同一短生命周期 E2E 环境一次只执行一个测试运行，后一个运行只能在前一个运行回收并恢复基线后开始。
_Avoid_: 并发 E2E, 共享并行测试

**空工作区**：仅含一个测试身份、没有活动列表或任务的具名场景，是每个测试运行的起始基线。
_Avoid_: 空数据库, 默认数据

**运行归属数据**：在空工作区场景中，由测试身份创建、`createdBy` 等于该身份 ID 的持久化产品记录；测试运行回收只可物理删除这些记录。
_Avoid_: 整表测试数据, 任意用户数据
