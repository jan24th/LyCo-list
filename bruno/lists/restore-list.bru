meta {
  name: restore list
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/lists/{{listId}}/restore
  body: json
  auth: bearer
}

auth:bearer {
  token: {{process.env.BRUNO_ACCESS_TOKEN}}
}

body:json {
  {
    "expectedVersion": 3
  }
}
