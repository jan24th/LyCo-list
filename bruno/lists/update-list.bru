meta {
  name: update list
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/lists/{{listId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{process.env.BRUNO_ACCESS_TOKEN}}
}

body:json {
  {
    "name": "新名称",
    "expectedVersion": 1
  }
}
