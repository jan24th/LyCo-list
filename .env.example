# AWS profile used by SST deployments
AWS_PROFILE=lyco-list-sst

# Base domain for production custom domains (only applied when SST stage === "prod")
# Copy this file to .env or .env.prod and fill in the actual value.
# API will use api.<BASE_DOMAIN> and web will use app.<BASE_DOMAIN>.
BASE_DOMAIN=jan24th.today
