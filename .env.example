# AWS profile used by SST deployments
AWS_PROFILE=lyco-list-sst

# Production custom domains (only applied when SST stage === "prod")
# Copy this file to .env or .env.prod and fill in the actual values.
API_DOMAIN=api.jan24th.today
WEB_DOMAIN=app.jan24th.today
